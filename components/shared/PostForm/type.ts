export type FormProps = {
    className?: string
}
// Type option for single-select
export type SelectorOption = {
    id: string
    name: string
    intName: string
}

// Type option for multi-select
export type MultiSelectorOption = {
    id: string
    name: string
}